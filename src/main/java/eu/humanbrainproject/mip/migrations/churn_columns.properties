# suppress inspection "UnusedProperty" for whole file

# Name of the target table
__TABLE=churn
# Columns of the table
__COLUMNS=state,account_length,area_code,phone,intl_plan,vmail_plan,vmail_message,day_mins,day_calls,day_charge,eve_mins,eve_calls,eve_charge,night_mins,night_calls,night_charge,intl_mins,intl_calls,intl_charge,custserv_calls,churn

# Description of the type and constraints for each column in the table
state.type=char(2)
account_length.type=int
area_code.type=varchar(20)
phone.type=varchar(20)
intl_plan.type=varchar(20)
vmail_plan.type=varchar(20)
vmail_message.type=varchar(20)
day_mins.type=numeric
day_calls.type=int
day_charge.type=numeric
eve_mins.type=numeric
eve_calls.type=int
eve_charge.type=numeric
night_mins.type=numeric
night_calls.type=int
night_charge.type=numeric
intl_mins.type=numeric
intl_calls.type=int
intl_charge.type=numeric
custserv_calls.type=int
churn.type=varchar(20)
